import { registerThemes } from './themes';

// External contributors can add theme modules and register them here.
// Keep ids as stable lowercase slugs because they are stored in chrome.storage.
registerThemes([]);
